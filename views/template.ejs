<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        footer, header {
            align-items: center;
            background-color: aqua;
            display: flex;
            flex-direction: column;
            justify-content: center;
            min-height: 50px;
            padding: 5px;
        }
        .database {
            background-color: black;
            color: white;
            margin: 5px;
            padding: 5px;
        }
        .database-name {
            font-size: 1.2em;
            text-align: center;
        }
    </style>
</head>
<body>
    <%- include("header") %>

    <h2>EJS template</h2>
    
    <p>This is the only page of this app using EJS/templating.</p>

    <p>The items below come from a MongoDB database.</p>

    <p><strong>If nothing shows up below in black boxes</strong>, go back to the main page of the app and populate the database.</p>

    <div>
        <% for (let feedback of feedbacks) { %>
            <div class="database">
                <p class="database-name"><%= feedback.name %></p>
                <p><%= feedback.name %> likes the number <%= feedback.number %>.</p>
                <p><%= feedback.name %> has a message for you. There it is: "<%= feedback.comment %>".</p>
                <p>You can spam <%= feedback.name %> at <%= feedback.email %>. <%= feedback.name %> loves spam.</p>
            </div>
        <% } %>
    </div>
    
    <%- include("footer") %>
</body>
</html>


